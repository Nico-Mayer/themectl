{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/Nico-Mayer/themectl-cli/main/schemas/variant.schema.json",
  "properties": {
    "appearance": {
      "type": "string",
      "enum": [
        "dark",
        "light"
      ],
      "description": "Appearance of this variant. Falls back to the family default; resolving fails if neither sets it."
    },
    "themes": {
      "additionalProperties": {
        "type": "string"
      },
      "type": "object",
      "description": "Per-integration theme names keyed by integration. Overrides the family defaults."
    },
    "wallpaper_sources": {
      "items": {
        "type": "string"
      },
      "type": "array",
      "uniqueItems": true,
      "description": "Extra wallpaper sources: theme ids (family/variant) or shared wallpaper dir names. The variant's own wallpaper dir is always included."
    }
  },
  "additionalProperties": false,
  "type": "object",
  "title": "themectl variant.toml",
  "description": "A single theme variant. Values not set here are inherited from the family's [defaults]."
}
