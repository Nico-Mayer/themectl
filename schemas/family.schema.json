{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/Nico-Mayer/themectl-cli/main/schemas/family.schema.json",
  "properties": {
    "defaults": {
      "properties": {
        "appearance": {
          "type": "string",
          "enum": [
            "dark",
            "light"
          ],
          "description": "Appearance of this variant. Falls back to the family default; resolving fails if neither sets it."
        },
        "themes": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object",
          "description": "Per-integration theme names keyed by integration. Overrides the family defaults."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "Defaults inherited by every variant; a variant may override any of them."
    }
  },
  "additionalProperties": false,
  "type": "object",
  "title": "themectl family.toml",
  "description": "Family-wide defaults inherited by every variant of the theme family."
}
